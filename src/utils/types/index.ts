type Tweet = {
  tweetText: string;
  tweetAuthorName: string;
  tweetCreatedAt: string;
};

type User = {
  email: string;
  name: string;
  points: number;
};

type Campaign = {
  id: string;
  campaignPhrase: string;
};
